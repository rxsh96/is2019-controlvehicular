import 'package:flutter/material.dart';

class FrenosPage extends StatefulWidget {
  FrenosPage({Key key}) : super(key: key);

  _FrenosPageState createState() => _FrenosPageState();
}

class _FrenosPageState extends State<FrenosPage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(child: Text('Frenos Page')),
    );
  }
}